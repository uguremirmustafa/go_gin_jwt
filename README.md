# Go Gin JWT Implementation

Basic JWT implementation in Gin web framework for authencication.

## Run locally

```bash
# start pg database via docker
docker compose up

# install packages
go mod download

# start server
go run .
```
